<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pcmazzters">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="SYS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.LoginActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MenuActivity"
            android:label="@string/actionbar_menu"
            android:parentActivityName=".activity.MenuActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.new_orders.NewOrdersActivity"
            android:label="Back to menu"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.CompleteOrders"
            android:label="Back to menu"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.ProfileActivity"
            android:label="Back to menu"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.AboutActivity"
            android:label="Back to menu"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.new_order_adder.ModelChooserActivity"
            android:label="Back to menu"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.new_order_adder.ProblemDescriberActivity"
            android:label="Back to menu"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.new_orders.OperationChooserActivity"
            android:screenOrientation="portrait"
            android:label="Back to current orders"/>
    </application>

</manifest>