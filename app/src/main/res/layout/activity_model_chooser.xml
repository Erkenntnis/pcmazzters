<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_model_chooser"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.pcmazzters.activity.new_order_adder.ModelChooserActivity">

    <android.support.v7.widget.Toolbar
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:id="@+id/action_bar_model_chooser"
        android:elevation="4dp"
        android:background="#000000"
        android:navigationIcon="@color/colorWhite"
        />
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/action_bar_model_chooser"
        android:orientation="vertical">
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Please, choose the model of device:"
            android:textColor="#000000"
            android:gravity="center"
            android:textSize="30dp"
            android:id="@+id/textViewAddNew"
            android:layout_marginTop="10dp"
            />
        <ListView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/listViewModel"
            android:layout_marginTop="20dp"
            android:layout_marginLeft="20dp"
            android:textColor="@color/colorBlack"
            />
    </LinearLayout>
</RelativeLayout>
